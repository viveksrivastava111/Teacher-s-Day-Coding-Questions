#include <iostream>
#include <cmath>

using namespace std;

int main() {
    int t, n, m;
    cin >> t;
    while (t--) {
        cin >> n >> m;
        int ans = (n * m + 1) / 2; // Equivalent to ceil(n*m/2)
        cout << ans << endl;
    }
    return 0;
}
